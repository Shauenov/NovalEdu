events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    upstream api {
        server api:8000;
    }

    server {
        listen 80;
        server_name _;

        # Redirect HTTP to HTTPS in production
        # return 301 https://$host$request_uri;

        location /static/ {
            alias /static/;
        }

        location / {
            proxy_pass         http://api;
            proxy_set_header   Host $host;
            proxy_set_header   X-Real-IP $remote_addr;
            proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;

            # File upload size limit
            client_max_body_size 15M;
        }
    }

    # HTTPS block — uncomment in production after SSL setup
    # server {
    #     listen 443 ssl;
    #     server_name educonductor.college.edu.kz;
    #     ssl_certificate     /etc/letsencrypt/live/educonductor.college.edu.kz/fullchain.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/educonductor.college.edu.kz/privkey.pem;
    #
    #     location / {
    #         proxy_pass http://api;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         client_max_body_size 15M;
    #     }
    # }
}
